# .workshop
Contains all configurations to setup my preferred work environment, my "workshop".


## Setup
Clone this repo to `~/.workshop` and run `make`.
This will symlink all configurations into `~/`.
